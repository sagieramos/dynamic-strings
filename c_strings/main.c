#include <stdio.h>
#include "c_strings.h"

int main(int argc, char* argv[])
	{
		for(int i = 1; i < argc; i++){	
		runComm(argv[i]);
		printf("\tindex: %d\tvalue: %.2f\n", buf.index, buf.value);
		}
		return 0;
	}
