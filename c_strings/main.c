#include <stdio.h>
#include "c_strings.h"

int main(int argc, char* argv[])
	{
		for(int i = 1; i < argc; i++){	
		runComm(argv[i]);
		printf("\tvalue: %.2f\tindex: %d\n", buf.value, buf.index);
		}
		return 0;
	}
